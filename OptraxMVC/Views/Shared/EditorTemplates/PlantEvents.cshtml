@using OptraxDAL.Models.Grow

@model List<PlantEvent>

@for(int i = 0; i < Model.Count; i++)
{
    @if (Model[i] is TransferEvent transferModel)
    {
        @Html.EditorFor(m => transferModel, "TransferEvent", new ViewDataDictionary(ViewData))
    }
    @* @Html.EditorFor(m=> m[i], new ViewDataDictionary(ViewData)); *@
}